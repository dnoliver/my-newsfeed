goog.provide('app.publication.TextPublication');
goog.require('app.publication.Publication');

app.publication.TextPublication = function(){
  goog.base(this);
};

goog.inherits(app.publication.TextPublication,app.publication.Publication);