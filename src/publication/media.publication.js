goog.provide('app.publication.MediaPublication');
goog.require('app.publication.Publication');

app.publication.MediaPublication = function(){
  goog.base(this);
};

goog.inherits(app.publication.MediaPublication,app.publication.Publication);