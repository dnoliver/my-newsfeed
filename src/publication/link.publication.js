goog.provide('app.publication.LinkPublication');
goog.require('app.publication.Publication');

app.publication.LinkPublication = function(){
  goog.base(this);
};

goog.inherits(app.publication.LinkPublication,app.publication.Publication);